# GBC Demo Runner dependencies
# Install: pip install -r demo/requirements.txt

# Note: this runner talks to GBC only through the MCP server — it does not
# import the main GBC library. As long as the MCP tool interface is stable,
# the runner does not need changes when the main library's language/env changes.

rich>=13.0
typer>=0.9
pytest>=7.0
