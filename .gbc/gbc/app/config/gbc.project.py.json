{
    "provides": {
        "config.project.current_project_runtime_state": {
            "desc": "当前目标项目根在进程初始化时按 GBC_PROJECT_ROOT > cwd 固定，不向上猜；路径 expanduser+resolve，空环境回退 cwd；set_current_project(str|PathLike) 显式规范化覆盖且空值拒绝。",
            "test": "tests/test_config_project.py",
            "executor": "pytest-gbc-v2",
            "timeout_override": -1,
            "heavy": 0,
            "dependents": [
                "gbc/app/interface/cli.py"
            ],
            "disabled": false
        }
    },
    "depends_on": []
}